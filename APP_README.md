# ICL Workflow - 人在回路可视化系统

基于Streamlit开发的交互式ICL剥离工作流系统。

## 快速启动

```bash
# 方式1: 使用启动脚本
python launch_app.py

# 方式2: 直接使用streamlit
streamlit run app.py
```

应用将在 http://localhost:8501 打开

## 系统特点

### 1. 可视化参数调节
- **Step 1**: 滑块调整检测阈值、最小面积
- **Step 2**: 调整星芒宽度、长度、旋转角度
- **Step 3**: 调整膨胀系数和核形状
- **Step 4**: 选择插值方法

### 2. 实时预览
- 每步运行后立即显示可视化结果
- 对比原图、Mask、叠加效果
- 自动生成的分析图表

### 3. 人工审核流程
- 每步都有 ✅批准 / ❌拒绝 按钮
- 拒绝后可立即调整参数重新运行
- 支持手动排除区域（保护引力弧）

### 4. 质量评估
- Step 4自动计算质量指标
- 综合评分、通量守恒、形态保持
- 通过/未通过状态显示

## 界面截图

```
┌─────────────────────────────────────────────────────────────┐
│  🔭 ICL Workflow - 人在回路系统                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📍 导航          │  Step 1: 初始星系Mask提取                 │
│  ─────────────────├────────────────────────────────────────  │
│  ✅ 0: 上传        │                                          │
│  → 1: 初始Mask    │  [原图+Mask] [仅Mask] [可视化报告]         │
│    2: 星芒Mask    │  ╔══════════════════════════╗             │
│    3: 合并膨胀    │  ║                          ║             │
│    4: ICL剥离     │  ║   图像显示区域            ║             │
│    5: 完成        │  ║   (matplotlib图表)        ║             │
│                   │  ║                          ║             │
│  ⚙️ Step 1 参数   │  ╚══════════════════════════╝             │
│  ─────────────────├────────────────────────────────────────  │
│  检测阈值: 1.5   │  检测天体数: 2945          中心X: 561.4    │
│  [====●====]     │  ⚠️ 可能存在弧污染                          │
│                  │                                            │
│  最小面积: 5     │  人工审核                                  │
│  [====●====]     │  ┌──────────┐  ┌──────────┐               │
│                  │  │ ✅ 批准   │  │ ❌ 拒绝   │               │
│  [🚀 运行提取]    │  └──────────┘  └──────────┘               │
│                  │                                            │
│                  │  🔧 手动排除区域（保护引力弧）              │
│                  │  [展开/折叠]                               │
└─────────────────────────────────────────────────────────────┘
```

## 文件结构

```
icl-workflow/
├── app.py                 # Streamlit主应用
├── launch_app.py          # 启动脚本
├── APP_README.md          # 本文件
├── src/                   # 工作流核心代码
│   ├── tools.py
│   ├── workflow.py
│   └── utils.py
├── uploads/               # 上传的文件 (自动创建)
└── outputs/app/           # 输出结果 (自动创建)
```

## 使用流程

1. **Step 0**: 拖拽上传FITS文件 → 点击"评估场复杂度"
2. **Step 1**: 调整检测参数 → 运行 → 检查Mask → 批准/调整
3. **Step 2**: 调整星芒几何 → 运行 → 与星芒对齐 → 批准
4. **Step 3**: 调整膨胀系数 → 运行 → 检查覆盖范围 → 批准
5. **Step 4**: 选择插值方法 → 运行 → 查看质量评分 → 下载结果

## 高级功能

### 手动排除区域
在Step 1中，如果发现引力弧被错误地mask：
1. 展开"🔧 手动排除区域"
2. 输入弧的中心坐标和半径
3. 点击"添加排除区域"
4. 重新运行提取

### 参数推荐
- **低密度场** (复杂度 < 20): 使用 AUTO 模式，阈值 1.5
- **中密度场** (复杂度 20-50): 使用 SEMI_AUTO，阈值 2.0
- **高密度场** (复杂度 > 50): 使用 MANUAL，阈值 2.5-3.0

## 故障排除

**问题**: Streamlit页面加载缓慢  
**解决**: 大型FITS文件可能需要更长时间，建议使用较小的图像(≤1024×1024)

**问题**: Step 4运行超时  
**解决**: RBF插值对大图很慢，建议先用"linear"或"nearest"测试

**问题**: 内存不足  
**解决**: 减小图像尺寸或使用更小的膨胀系数

## 未来扩展

- [ ] 支持多波段同时处理
- [ ] 3D可视化（深度切片）
- [ ] 批量处理模式
- [ ] 结果对比工具
- [ ] 机器学习辅助检测
